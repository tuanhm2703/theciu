<div id="sticky-gift-icon" class="{{ $numberOfVouchers > 0 ? 'active' : '' }}">
    <strong id="number-of-voucher" class="{{ $numberOfVouchers == 0 ? 'd-none' : '' }}">{{ $numberOfVouchers }}</strong>
    <div class="my-btn-border"></div>
    <img src="/img/gift-icon.png" class="btn-bell" alt="">
</div>
